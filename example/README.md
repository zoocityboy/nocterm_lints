# nocterm_example

A minimal example showing how to use the `nocterm_lints` package for analysis.

Usage
 - Change directory into `example`
 - Install dependencies and run the analyzer:

```bash
cd example
dart pub get
dart analyze
dart run bin/main.dart
```

The `analysis_options.yaml` in this folder includes `package:nocterm_lints/recommended.yaml`.
